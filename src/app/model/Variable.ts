export class Variable {

    id: number;
    nombre: string;
    valor: string;

    constructor(id: number, nombre: string, valor: string) {

        this.id = id;
        this.nombre = nombre;
        this.valor = valor;
    }
}